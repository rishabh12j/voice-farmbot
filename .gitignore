# ROS2 build artefacts
/build/
/install/
/log/

# Compiled Python
*.pyc
__pycache__/
*.egg-info/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# GrowMate runtime data — do not commit (lives in ~/.growmate_voice/)
*.log
history.jsonl
demo_recordings/

# Local scratch
tools/_*.yaml
tools/_*.csv
tools/_*.py

# Personal / academic artefacts — do not commit.
demo/Interim_Report_*.pdf
*.pdf
*.pptx
documentation/*.docx
documentation/*.xlsx
documentation/*.zip

# The repo carries code, README, and RUNBOOK — what the thing is and how to run
# it. Everything below is thesis/agent working material that lives on the
# author's machine only. It is NOT required to run the code, and it is NOT
# backed up by this repo — keep your own copy.
/AGENTS.md
/CLAUDE.md
/HANDOVER.md
/PLANS.md
documentation/thesis.md
documentation/eval_v2_results.md
documentation/eval/
documentation/paper/
documentation/MU EE696/
# Community demo day: scanned questionnaire packets and named attendee
# transcripts. Personal data with no consent basis for publication — origin is
# a public remote. Never commit; use the anonymised themes in
# documentation/eval/older_adult_demo_feedback.md instead.
documentation/demo/
# Page images rendered from those scans (the PDFs are image-only, so reading
# them meant rasterising). Same personal data, same rule.
.tmp_feedback_pages/

# Agent scratch dirs
.codex-tmp/

# Virtual environments
venv/
venv-*/
.venv/

# Curl-downloaded API docs (regenerate with build_docs script)
src/growmate_voice/intent_server_docs*.html
src/growmate_voice/intent_server_openapi.json

# Loose root data files (real maps live in tools/maps/)
/map.yaml
/a.yaml
tools/active_map.yaml
