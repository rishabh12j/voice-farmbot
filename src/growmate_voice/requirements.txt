# Runtime dependencies for the growmate_voice package.
#
# Non-ROS2 Python deps. The ROS2 side (rclpy, std_msgs) comes from your
# sourced ROS2 workspace — do not pip-install those.
#
# Install with:
#   pip install -r src/growmate_voice/requirements.txt

# Config parsing
pyyaml>=6.0

# Web app (FastAPI + uvicorn replaces Gradio)
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Audio handling
numpy>=1.24
scipy>=1.10
soundfile>=0.12
sounddevice>=0.4.6

# STT backends — install whichever you plan to use
faster-whisper>=1.0
vosk>=0.3.45
useful-moonshine-onnx>=0.1.1

# TTS backends — optional
piper-tts>=1.2.0
kokoro>=0.8.0
